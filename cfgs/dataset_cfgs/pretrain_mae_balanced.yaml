kwargs: {
  train:  {
    use_cache: True,
    resample_num: 3000,
    rand_each_num: {
      obman: 60, # 30
      grabnet: 3000 # 1500
    }
  },
  val: {
    use_cache: True,
    resample_num: 3000,
    rand_each_num: {
      obman: 10, # 5
      grabnet: 500 # 250
    }
  }
}
